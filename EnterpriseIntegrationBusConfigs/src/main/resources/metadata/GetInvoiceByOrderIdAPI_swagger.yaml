openapi: 3.0.1
info:
  title: InvoiceDetailAPI
  description: API Definition of GetInvoiceByOrderIdAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/api/v1/invoices
paths:
  /{orderId}:
    get:
      parameters:
      - name: orderId
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
