openapi: 3.0.1
info:
  title: MomoWebhookAPI
  description: API Definition of MomoWebhookAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/api/v1/webhook
paths:
  /momo:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
